import { Search } from 'lucide-react'; import { Link } from 'react-router-dom'; import { useState } from 'react';
import { Avatar, Button, Card, Input, PageHeader, PageState, Pagination, Select, StatusBadge } from '../components/ui'; import { useRemote } from '../hooks/useRemote'; import { mechanicsApi } from '../services/resources';
const initial = { search:'', status:'', specialization:'', sort:'name', page:1, limit:12 };
export default function MechanicsPage(){const[draft,setDraft]=useState(initial);const[query,setQuery]=useState(initial);const{data,loading,error}=useRemote(()=>mechanicsApi.list(query),[JSON.stringify(query)]);const apply=(e)=>{e.preventDefault();setQuery({...draft,page:1})};return <><PageHeader title="Mechanics" description="Monitor workforce availability, skills, and active assignments."/><Card className="mb-5 p-4"><form onSubmit={apply} className="grid gap-3 sm:grid-cols-2 xl:grid-cols-5"><div className="relative xl:col-span-2"><Search className="absolute left-3 top-3 h-4 text-muted-foreground"/><Input className="pl-9" placeholder="Search name, email, phone…" value={draft.search} onChange={e=>setDraft({...draft,search:e.target.value})}/></div><Select value={draft.status} onChange={e=>setDraft({...draft,status:e.target.value})}><option value="">All statuses</option><option>available</option><option>busy</option><option>offline</option></Select><Input placeholder="Specialization" value={draft.specialization} onChange={e=>setDraft({...draft,specialization:e.target.value})}/><div className="flex gap-2"><Select value={draft.sort} onChange={e=>setDraft({...draft,sort:e.target.value})}><option value="name">Name A–Z</option><option value="-jobsCompleted">Most jobs</option><option value="status">Status</option><option value="-createdAt">Newest</option></Select><Button>Apply</Button></div></form></Card>{loading?<PageState/>:error?<PageState type="error" message={error}/>:!data.mechanics.length?<PageState type="empty" message="No mechanics found."/>:<Card className="overflow-hidden"><div className="grid gap-px bg-border sm:grid-cols-2 xl:grid-cols-3">{data.mechanics.map(m=><Link key={m._id} to={`/mechanics/${m._id}`} className="bg-card p-5 hover:bg-muted/40"><div className="flex items-start justify-between"><div className="flex gap-3"><Avatar name={m.name} src={m.profileImage}/><div><p className="font-semibold">{m.name}</p><p className="text-xs text-muted-foreground">{m.email}</p></div></div><StatusBadge status={m.status}/></div><div className="mt-5 flex items-center justify-between text-sm"><span><b>{m.jobsCompleted}</b> jobs completed</span><span className="text-muted-foreground">{m.currentBooking?m.currentBooking.bookingId:'No active job'}</span></div><div className="mt-3 flex flex-wrap gap-1">{m.specializations.map(s=><span key={s} className="rounded bg-muted px-2 py-1 text-xs">{s}</span>)}</div></Link>)}</div><Pagination pagination={data.pagination} onPage={page=>setQuery({...query,page})}/></Card>}</>}
