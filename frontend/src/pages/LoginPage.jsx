import { Eye, EyeOff, ShieldCheck, Wrench } from 'lucide-react';
import { useState } from 'react';
import { Button, Input, Spinner } from '../components/ui';
import { useAuth } from '../context/AuthContext';
import { getErrorMessage } from '../services/api';
export default function LoginPage() { const { login } = useAuth(); const [form, setForm] = useState({ email: '', password: '' }); const [show, setShow] = useState(false); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); const submit = async (event) => { event.preventDefault(); setError(''); if (!form.email.trim() || !form.password) return setError('Email and password are required.'); setLoading(true); try { await login({ email: form.email.trim(), password: form.password }); } catch (e) { setError(getErrorMessage(e)); } finally { setLoading(false); } }; return <div className="grid min-h-screen bg-card lg:grid-cols-2"><div className="hidden bg-slate-950 p-14 text-white lg:flex lg:flex-col"><div className="flex items-center gap-3 text-xl font-bold"><span className="grid h-10 w-10 place-items-center rounded-xl bg-primary"><Wrench /></span>Instant Mechanic</div><div className="my-auto max-w-xl"><p className="mb-4 text-sm font-semibold uppercase tracking-[.25em] text-emerald-400">Operations command center</p><h1 className="text-5xl font-bold leading-tight">Every roadside service, clearly in view.</h1><p className="mt-6 text-lg text-slate-300">Coordinate bookings, mechanics, customers, and revenue from one live workspace.</p></div><p className="text-sm text-slate-400">Secure administrative access</p></div><div className="grid place-items-center bg-background p-6"><form onSubmit={submit} className="w-full max-w-md rounded-2xl border bg-card p-8 shadow-xl"><div className="mb-8"><span className="mb-5 grid h-12 w-12 place-items-center rounded-xl bg-primary/10 text-primary"><ShieldCheck /></span><h2 className="text-2xl font-bold">Welcome back</h2><p className="mt-2 text-sm text-muted-foreground">Sign in to the operations dashboard.</p></div>{error && <div className="mb-4 rounded-lg border border-red-200 bg-red-50 p-3 text-sm text-red-700 dark:border-red-900 dark:bg-red-950/30 dark:text-red-300">{error}</div>}<label className="mb-2 block text-sm font-medium">Email address</label><Input type="email" autoComplete="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} placeholder="admin@example.com" /><label className="mb-2 mt-5 block text-sm font-medium">Password</label><div className="relative"><Input type={show ? 'text' : 'password'} autoComplete="current-password" value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} className="pr-11" /><button type="button" className="absolute right-3 top-2.5 text-muted-foreground" onClick={() => setShow(!show)} aria-label="Show or hide password">{show ? <EyeOff className="h-5" /> : <Eye className="h-5" />}</button></div><Button className="mt-6 w-full" disabled={loading}>{loading && <Spinner className="h-4 w-4" />}{loading ? 'Signing in…' : 'Sign in'}</Button></form></div></div>; }
